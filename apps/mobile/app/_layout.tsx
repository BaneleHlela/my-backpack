import { Slot } from 'expo-router';

// Phase 1 placeholder — Redux <Provider> and auth-state-aware guards are
// wired up in later phases once the store exists.
export default function RootLayout() {
  return <Slot />;
}
