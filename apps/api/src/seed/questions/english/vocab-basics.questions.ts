// Starter file for pre-seeded English vocabulary terms (manually written, not from the
// dictionary API) to populate the dictionary before users start searching their own words.
// Add terms here following the same pattern as isizulu/vowels.questions.ts.

export async function seedEnglishVocabBasics(_dictionaryMiniAppId: string): Promise<void> {
  console.log('Seeding English vocabulary basics...');
  // Empty for now — structure ready for you to add terms following the same pattern
  // as vowels.questions.ts. This does NOT use the dictionary API — these are
  // pre-seeded terms with manually written definitions.
  console.log('  No basics seeded yet — add terms to this file');
}
